{{- if and .Values.crds.managed .Values.crds.clusternetworktopologies }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusternetworktopologies.scheduling.koordinator.sh
spec:
  group: scheduling.koordinator.sh
  names:
    kind: ClusterNetworkTopology
    listKind: ClusterNetworkTopologyList
    plural: clusternetworktopologies
    singular: clusternetworktopology
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              networkTopologySpec:
                items:
                  properties:
                    labelKey:
                      description: |-
                        LabelKey is the key of label on the Node which represents the layer that the Node belongs to.
                        The matching policy is any of.
                      items:
                        type: string
                      type: array
                    parentTopologyLayer:
                      description: ParentTopologyLayer is the type of the parent layer
                        of the layer.
                      type: string
                    topologyLayer:
                      description: TopologyLayer is the type of the layer.
                      type: string
                  required:
                  - topologyLayer
                  type: object
                type: array
            required:
            - networkTopologySpec
            type: object
          status:
            properties:
              detailStatus:
                items:
                  properties:
                    childTopologyLayer:
                      type: string
                    childTopologyNames:
                      items:
                        type: string
                      type: array
                    nodeNum:
                      format: int32
                      type: integer
                    parentTopologyInfo:
                      properties:
                        topologyLayer:
                          type: string
                        topologyName:
                          type: string
                      required:
                      - topologyLayer
                      - topologyName
                      type: object
                    topologyInfo:
                      properties:
                        topologyLayer:
                          type: string
                        topologyName:
                          type: string
                      required:
                      - topologyLayer
                      - topologyName
                      type: object
                  required:
                  - nodeNum
                  - topologyInfo
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

{{- end }}
